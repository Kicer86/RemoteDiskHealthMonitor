
#include <QJsonDocument>

#include "common/GeneralHealth.h"

class AgentStatus
{
    PROP(GeneralHealth::Health overallStatus = GeneralHealth::Health::UNKNOWN);
    PROP(QJsonDocument diskInfoCollection)

    SLOT(refresh());
};
