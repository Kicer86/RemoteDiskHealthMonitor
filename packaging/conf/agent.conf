# RDHM Agent configuration
# This file is sourced by the systemd unit as EnvironmentFile.

# Agent name visible via ZeroConf/mDNS discovery.
# Defaults to "RDHAgent" if left empty.
AGENT_NAME=RDHAgent
